# Temp snapshot — genesis-mythos-master

Snapshot of all files under this project’s subdirs (Roadmap tree + root MOC), created for reference or iteration backup.

- **Created:** 2026-03-14  
- **Contents:** Copy of `Roadmap/` (full tree) and `genesis-mythos-master-Roadmap-MOC.md`. `.git` excluded.  
- **Refresh:** To update this snapshot after new files are generated during iteration, re-run the same copy (e.g. rsync from project root excluding `.git` and `temp` into `temp/`).
